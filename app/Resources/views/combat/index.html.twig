{% extends 'base.html.twig' %}

{% block header %}
{% endblock %}
{% block body %}
    <h1 style="text-align: center">Combats list</h1>
    <a href="{{ path('user_new') }}" style="text-align: center">Back to the admin page</a>
    <table class="table">
        <thead>
            <tr class="myHead">
                <th>Id</th>
                <th>Date</th>
                <th>Resultat</th>
                <th>Actions</th>
                <th>Equipe 1</th>
                <th>Equipe 2</th>

            </tr>
        </thead>
        <tbody>
        {% for combat in combats %}
            <tr>
                <td><a href="{{ path('combat_show', { 'id': combat.id }) }}">{{ combat.id }}</a></td>
                <td>{% if combat.date %}{{ combat.date|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ combat.resultat }}</td>
                <td>{{ combat.equipe1.nomEquipe }}</td>
                <td>{{ combat.equipe2.nomEquipe }}</td>


                <td>
                    <ul>
                        <li>
                            <a href="{{ path('combat_show', { 'id': combat.id }) }}" class="btn btn-info playBtn">show</a>
                        </li>
                        <li>
                            <a href="{{ path('combat_edit', { 'id': combat.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('combat_new') }}">Create a new combat</a>
        </li>
    </ul>
{% endblock %}
